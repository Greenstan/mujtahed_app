class SubjectModel {
  String name;
  String description;
  int difficulty;
  bool isCompleted;

  SubjectModel({
    required this.name,
    required this.description,
    required this.difficulty,
    required this.isCompleted,
  });
}
